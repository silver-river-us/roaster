# Super Admin Credentials
# These credentials are used to access /super-admin for managing organizations
SUPER_ADMIN_USERNAME=admin
SUPER_ADMIN_PASSWORD=changeme

# Session Secret
# Used for encrypting session cookies. Generate a secure random string for production.
# If not set, a random secret will be auto-generated on each server restart.
# To generate: ruby -e "require 'securerandom'; puts SecureRandom.hex(32)"
SESSION_SECRET=
